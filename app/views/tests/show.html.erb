  <h1> List questions of test "<%= @test.title %>" </h1>

  <table>
    <tr>
      <th>Body</th>
      <th colspan="3">Actions</th>
    </tr>

    <%= render @questions %>

  </table>

  <%= link_to 'New question', "/tests/#{params['id']}/questions/new" %>
