<h1><%= t('.header')%></h1>

<table>
  <tr>
    <th> <%= t('common.title') %></th>
    <th> <%= t('tests.index.questions') %></th>
    <th colspan="3"><%= t('common.actions') %></th>
  </tr>

  <%= render @tests %>

</table>

<p>
  <%= link_to t('tests.index.new'), new_admin_test_path %>
</p>
